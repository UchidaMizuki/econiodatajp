industry_total_pattern <- "内生部門計"
value_added_total_pattern <- "粗付加価値部門計"
final_demand_total_pattern <- "[都道府県]内最終需要計"

export_pattern <- "[移輸]出"
export_total_pattern <- "(移輸|輸移)出計"

import_pattern <- "[（\\(]控除[）\\)]([移輸]入|関税・輸入品商品税)"
import_total_pattern <- "[（\\(]控除[）\\)](移輸|輸移)入計"

total_pattern <- "[都道府県]内生産額"
