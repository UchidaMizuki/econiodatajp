% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sector-target.R
\name{io_sector_target}
\alias{io_sector_target}
\alias{io_sector_target_raw}
\title{Declare a target that reads a sector classification}
\usage{
io_sector_target(
  name,
  region_type = c("regional", "multiregional"),
  region_class = c("nation", "pref", "block"),
  year,
  axis = c("input", "output"),
  ...
)

io_sector_target_raw(
  name,
  region_type = c("regional", "multiregional"),
  region_class = c("nation", "pref", "block"),
  year,
  axis = c("input", "output"),
  ...
)
}
\arguments{
\item{name}{Symbol (`io_sector_target()`) or string (`io_sector_target_raw()`),
name of the target.}

\item{region_type}{Table "shape" whose sector classification to fetch,
`"regional"` (the default) or `"multiregional"` -- see [io_table_get()].}

\item{region_class}{Region granularity whose sector classification to
fetch, `"nation"` (the default), `"pref"`, or `"block"` -- see
[io_table_get()]. Not every `region_type`/`region_class`/`year`
combination has sector data; see [io_sector_available] to list which do.}

\item{year}{Year of the data.}

\item{axis}{Which axis of the classification to fetch, `"input"` or
`"output"`. The two aren't identical: e-stat's classification workbook
omits a handful of industry codes (memo items such as scrap-material
rows) from the output side, so `axis = "output"` backfills them from the
input axis, matching how the real IO table labels those sectors
identically on both axes.}

\item{...}{Arguments passed to [tarchives::tar_target_archive_raw()].}
}
\value{
A target definition object. Users should not modify these directly,
just feed them to \code{\link[=list]{list()}} in your target script file
(default: \verb{_targets.R}).
}
\description{
For use inside a `_targets.R` pipeline, e.g. `tar_plan(io_sector_target(...))`.
`io_sector_target()` uses non-standard evaluation to capture `name`, mirroring
[tarchives::tar_target_archive()]; `io_sector_target_raw()` takes `name` as a
string instead, for programmatic use such as building several targets in a
loop, mirroring [tarchives::tar_target_archive_raw()].
}
