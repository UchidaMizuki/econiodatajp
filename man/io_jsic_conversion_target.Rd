% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sector-target.R
\name{io_jsic_conversion_target}
\alias{io_jsic_conversion_target}
\alias{io_jsic_conversion_target_raw}
\title{Declare a target that reads the JSIC classification's own hierarchy
crosswalk}
\usage{
io_jsic_conversion_target(
  name,
  region_type = c("regional", "multiregional"),
  region_class = c("nation", "pref", "block"),
  year,
  region = NULL,
  ...
)

io_jsic_conversion_target_raw(
  name,
  region_type = c("regional", "multiregional"),
  region_class = c("nation", "pref", "block"),
  year,
  region = NULL,
  ...
)
}
\arguments{
\item{name}{Symbol (`io_jsic_conversion_target()`) or string
(`io_jsic_conversion_target_raw()`), name of the target.}

\item{region_type, region_class}{Currently only `region_type =
"regional"`, `region_class = "nation"` pipelines carry this data, and not
for every `year` [io_sector_jsic_get()] covers -- e.g. no
machine-readable source was found for `year = 2011`'s JSIC revision as
of writing. See [io_sector_available] to confirm which `year`s do.}

\item{year}{Year of the data.}

\item{region}{Which single region to select within `region_class`,
matching [io_table_get()]'s `region`. Defaults to `NULL`, which resolves
to `"nation"` -- as of writing, sector data only exists for `region_class
= "nation"` pipelines, so there's no other value to pass yet; the
argument exists so a future `region_class = "pref"`/`"block"` sector
archive (keyed per-prefecture/per-block the same way [io_table_get()]'s
tables are) wouldn't need a signature change to support it.}

\item{...}{Arguments passed to [tarchives::tar_target_archive_raw()].}
}
\value{
A target definition object. Users should not modify these directly,
just feed them to \code{\link[=list]{list()}} in your target script file
(default: \verb{_targets.R}).
}
\description{
For use inside a `_targets.R` pipeline, e.g.
`tar_plan(io_jsic_conversion_target(...))`.
`io_jsic_conversion_target()` uses non-standard evaluation to
capture `name`, mirroring [tarchives::tar_target_archive()];
`io_jsic_conversion_target_raw()` takes `name` as a string
instead, for programmatic use such as building several targets in a
loop, mirroring [tarchives::tar_target_archive_raw()].
}
